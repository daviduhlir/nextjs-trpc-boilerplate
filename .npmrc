# npm configuration
legacy-peer-deps=false
engine-strict=false

# Suppress deprecation warnings from transitive dependencies
# These come from Next.js build tools (glob, rimraf) and don't affect runtime
# They are maintained upstream and will be resolved in future Next.js versions
audit-level=moderate

# Optional: suppress warnings during install
# npm warn deprecated messages are informational only and don't affect functionality
